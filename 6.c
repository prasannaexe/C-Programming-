// program to check how modulo and power operators are used
# include<stdio.h>
# include<math.h>
int main(){
    int power,modulo;
    power = pow(2,3);
    printf ("power is %d \n", power);
    modulo = 7%3;
    printf ("modulo is %d ",modulo);
    return 0;
}