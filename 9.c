//program to print true if its sunday and its snowing
# include <stdio.h>
int main (){
    int isSunday =0;
    int isSnowing =0;
    printf ("%d", isSunday && isSnowing);
    return 0;
}
// the user have to input in this code itself they cant input in terminal